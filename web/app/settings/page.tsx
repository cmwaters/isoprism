"use client";

import { SettingsView } from "@/components/settings/settings-view";

// SettingsPage renders the settings page for the settings route.
export default function SettingsPage() {
  return <SettingsView account="settings" />;
}
