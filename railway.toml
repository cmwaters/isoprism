[build]
builder = "NIXPACKS"
buildCommand = "go build -o /app/server ./cmd/api"

[deploy]
startCommand = "/app/server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
